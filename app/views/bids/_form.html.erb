<%= form_with model: @bid do |f| %>
	<%= f.hidden_field :user_id, value: @user.id %>	
	<%= f.hidden_field :item_id, value: @item.id %>	

	<%= f.label :amount, "Place Bid: $" %>
	<%= f.text_field :amount %>

	<%= f.submit "Place Bid" %>		
<% end %>